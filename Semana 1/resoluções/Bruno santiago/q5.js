const x = 0.3
const y = 0.5

 if((x >=0 && y >= 0) && Math.pow(x,2)+ Math.pow(y,2)<=1)
 {
    console.log("A coordenada está na figura")
 }else{
    console.log("As coordenadas não estão na figura")
 }
